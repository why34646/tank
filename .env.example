# 坦克动荡 - 环境变量模板
# 复制此文件为 .env 并按需修改

# 远程版本检查（可选，架构阶段暂未启用）
# VERSION_CHECK_URL=https://example.com/tanktrouble/version.json

# 日志级别（DEBUG / INFO / WARNING / ERROR）
LOG_LEVEL=INFO

# 联机默认端口（可覆盖 settings 中的默认值）
# NETWORK_DEFAULT_PORT=7788
# NETWORK_DISCOVERY_PORT=7789
