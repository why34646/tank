# 【坦克动荡】技术栈选型文档

> 生成日期：2026-08-29
> 状态：已锁定
> 完善记录：本次修订/补充处已用【补充】【修订】标注

## 一、项目概况

- **项目类型**：Windows 桌面 2D 休闲对战游戏
- **规模**：单人开发（AI 主导），中小规模完整成品
- **性能需求**：低——2D 俯视角、简单几何图形、最多 6 人局域网联机
- **特殊需求**：局域网联机（房间发现 + 实时同步）、固定窗口、安装包、升级检测、一键卸载
- **选型原则**：适合优先 + AI 熟悉度优先，确定后不摇摆

## 二、技术栈总览

| 层级 | 技术选型 | 版本 |
|---|---|---|
| 开发语言 | Python | 3.11+ |
| 游戏引擎/渲染 | Pygame | 2.5+ |
| UI 组件库 | pygame_gui | 0.6+ |
| 网络通信 | 标准库 socket（UDP 广播 + TCP） | — |
| 数据序列化 | JSON | — |
| 本地存储 | JSON 文件（%APPDATA%） | — |
| 打包 exe | PyInstaller | 6.0+ |
| 安装/卸载程序 | Inno Setup | 6.0+ |
| 升级检测 | Inno Setup 覆盖安装 + 游戏内远程版本比对 | — |

## 三、各层选型理由

### 3.1 游戏引擎与语言：Python + Pygame

**最终选择：Python 3.11+ + Pygame 2.5+**

| 候选方案 | 优势 | 劣势 | 为何不选 |
|---|---|---|---|
| Python + Pygame | AI 最熟悉、开发极快、2D渲染/输入/碰撞够用、socket标准库直接联网、代码可读性高 | 无内置物理引擎、打包体积30-50MB、高DPI需额外处理 | —（选中） |
| Godot 4 (GDScript) | 专业引擎、内置物理和网络RPC、打包方便 | GDScript AI代码质量不如Python、引擎概念有学习成本 | AI熟悉度和调试效率不如Pygame |
| TypeScript + Electron + Canvas | UI好做、AI熟悉TS | 包体100MB+、内存高、游戏循环性能不如原生 | 对纯游戏场景过度设计 |
| Unity / C# | 生态最强 | 对简单几何图形游戏严重过度设计、工程复杂 | 杀鸡用牛刀 |
| C++ + SFML | 性能好 | 开发效率低、编译配置麻烦 | 不符合AI主导快速开发 |

**选择理由**：
- 本游戏为 2D 俯视角、矩形碰撞、炮弹向量反弹，不需要物理引擎，Pygame 完全覆盖
- AI 对 Pygame 的游戏循环、精灵、碰撞、事件处理代码质量最高，出 bug 易定位
- 局域网联机用标准库 socket 即可，无需第三方网络库
- Python 代码可读性最好，符合用户"自己看代码调试"的约束
- 高 DPI 问题通过 `ctypes.windll.user32.SetProcessDPIAware()` 解决，有成熟方案

### 3.2 UI 组件库：pygame_gui

**最终选择：pygame_gui 0.6+**

| 候选方案 | 优势 | 劣势 | 为何不选 |
|---|---|---|---|
| pygame_gui | 功能最全（按钮/输入框/下拉/列表/滑块/面板/对话框/聊天窗口）、主题JSON配置、与Pygame事件循环无缝集成、活跃维护 | API面较大需学习 | —（选中） |
| pygame-menu | 轻量上手快 | 主要面向菜单页，复杂布局（房间内聊天+成员+设置）不够灵活 | 房间内界面需求超出其能力 |
| 纯手写UI组件 | 完全可控、无依赖 | 全部控件自己写，工作量大、易出bug | 开发成本过高 |

**选择理由**：
- 直接覆盖所有 UI 场景：主菜单按钮、设置页下拉/单选、代号/密码输入框、房间列表、成员列表、多选设置、聊天框、游戏内HUD
- 主题用 JSON 配置颜色字体，无需手绘控件
- 可与 Pygame 原生绘制混用（游戏内 HUD 可用原生绘制更轻量）

### 3.3 网络通信：原生 socket（UDP 广播 + TCP）

**最终选择：Python 标准库 socket，UDP 广播做房间发现，TCP 做全部数据通信**

| 候选方案 | 优势 | 劣势 | 为何不选 |
|---|---|---|---|
| 原生 socket（UDP广播+TCP） | 无依赖、AI最熟悉、完全可控、局域网TCP延迟够用 | 需自己处理粘包/序列化/断线 | —（选中） |
| 原生 socket（UDP广播+TCP信令+UDP同步） | 游戏同步延迟最低 | 需处理UDP丢包/乱序，对6人休闲游戏过度设计 | 增加不必要的复杂度 |
| PodSixNet | 专为Pygame设计、事件驱动 | 项目老旧、维护停滞、文档少 | AI经验有限，风险高 |
| pyenet (ENet) | 游戏级网络库、可靠/不可靠混合 | 需编译C库、Windows安装坑多 | 安装复杂度不值得 |

**选择理由**：
- 局域网 6 人休闲游戏，TCP 延迟 1-5ms，可靠传输不影响手感，省掉 UDP 丢包重传代码
- 架构清晰：房主开 TCP 服务端 + UDP 广播监听；客户端 UDP 广播探测房间，选房后建立 TCP 连接；所有后续通信走 TCP
- 消息格式：`{"type": "xxx", "data": {...}}`，JSON 序列化，换行或固定长度头分隔解决粘包
- 需自行处理：房主掉线、玩家断线（保存战绩后其余人继续）、连接异常

### 3.4 本地存储：JSON 文件

**最终选择：JSON 文件，存于 `%APPDATA%/TankTrouble/`**

- 战绩数据（胜场、存活数、击杀数、最高连杀、对局时长、使用模式）结构简单，JSON 完全够用
- 无需引入 SQLite 等数据库，减少依赖
- 存于 AppData 目录，不随游戏安装目录变动，卸载时可配置保留

### 3.5 打包 / 安装 / 升级 / 卸载

**最终选择：PyInstaller + Inno Setup**

| 候选方案 | 优势 | 劣势 | 为何不选 |
|---|---|---|---|
| PyInstaller + Inno Setup | 最经典成熟、免费、文档全、AI最熟悉、自动生成卸载 | PyInstaller偶尔杀毒误报 | —（选中） |
| Nuitka + Inno Setup | 性能好、误报少、难反编译 | 编译慢、配置复杂、兼容性坑多 | 本项目不需要C级性能 |
| PyInstaller + NSIS | 脚本极灵活 | NSIS语法晦涩 | AI熟悉度低于Inno Setup |

**各环节实现方式**：

1. **打包 exe**：PyInstaller `--onedir` 目录模式（启动比单文件快），入口 `main.py`，自动收集 Pygame、pygame_gui 依赖
2. **安装程序**：Inno Setup 脚本配置安装目录、桌面/开始菜单快捷方式、注册表卸载项；同一 AppId 运行安装包自动检测旧版本并覆盖升级
3. **升级检测**：
   - 安装程序层：Inno Setup 同 AppId 覆盖安装即升级
   - 游戏内层：启动时读取远程 `version.json` 对比本地版本号，有新版本弹窗提示并打开下载链接
4. **一键卸载**：Inno Setup 自动生成 `unins000.exe` 于安装目录，注册到控制面板；额外创建"卸载坦克动荡"快捷方式
5. **版本号管理**：代码中 `VERSION = "x.x.x"`，主界面显示；Inno Setup `AppVersion` 同步；远程 `version.json` 存最新版本和下载地址

## 四、风险与注意事项

- **技术栈锁定声明**：本技术栈确认后不再随意变更，如需更换需评估迁移成本
- **潜在风险**：
  1. PyInstaller 打包的 exe 可能被 Windows Defender 或第三方杀毒误报——可通过代码签名缓解（需证书），或在文档中说明
  2. pygame_gui 与 Pygame 原生绘制混用时需注意渲染层级和事件传递顺序
  3. TCP 粘包处理必须在项目初期就封装好统一的消息收发工具类，避免后期重构
  4. 房主掉线后房间解散的逻辑需明确，游戏进行中房主掉线的处理需设计
- **降级方案**：
  - pygame_gui 如遇不可解决问题，核心 UI 可退化为手写轻量组件
  - 如 PyInstaller 误报严重，可评估 Nuitka 替代

## 五、不做的技术决策

- **不引入物理引擎**（如 Pymunk）：本游戏碰撞和反弹逻辑简单，自写 AABB + 向量反射即可，减少依赖
- **不引入游戏框架**（如 Arcade、Pyglet）：Pygame 已足够，额外框架增加学习成本
- **不使用 WebSocket / HTTP 服务器**：局域网纯 P2P（房主即服务端），不需要中心服务器
- **不使用数据库**：战绩数据量小，JSON 文件足够
- **不做反编译保护**：休闲游戏无需考虑，避免增加打包复杂度

## 六、再评估触发条件【补充】

本技术栈已锁定，仅在以下任一条件出现时才重新评估技术路线：

| 触发条件 | 说明 |
| --- | --- |
| 超过 6 人或跨网联机需求 | 若未来需要 7 人以上或广域网联机，房主即服务端的 TCP 方案需重新评估（可能引入专用服务器或 UDP 同步） |
| 性能瓶颈 | 若大量炮弹 / 特效导致帧率明显下降（如持续低于 60 FPS），考虑更底层渲染或 C 扩展 |
| pygame_gui 不可解决 | 若 pygame_gui 出现无法修复的阻塞问题，核心 UI 退化为手写轻量组件（对应"四、风险"降级方案） |
| PyInstaller 误报严重 | 若杀毒误报无法通过代码签名缓解，评估 Nuitka 替代 |
| 依赖许可变化 | 若 Pygame 等依赖的许可证发生不利于免费分发的变更，需重新评估（见"七、许可证检查"） |

## 七、许可证检查【补充】

本项目定位为"可能公开免费分发"。对关键依赖的许可证核对如下（均允许免费公开分发，无版权费）：

| 依赖 | 许可证 | 对免费分发的影响 |
| --- | --- | --- |
| Python 3.11+ | PSF License | 允许自由使用、修改、再分发（含商用），无传染性 |
| Pygame 2.5+ | LGPL 2.1+ | 允许免费分发与商用；你的游戏代码可闭源，但需附 LGPL 许可文本、允许用户替换 / 重链 Pygame 库；若修改 Pygame 本身，修改部分需开源 |
| pygame_gui 0.6+ | MIT | 最宽松，可任意使用 / 商用 / 闭源 |
| PyInstaller 6.0+ | GPL-2.0（含 PyInstaller 例外） | 打包产物可用你自己的许可分发，你的代码无需开源 |
| Inno Setup 6.0+ | 免费许可 | 允许免版税用于商业与非商业分发 |

**分发时需注意**：在安装包或说明文档中附带 Pygame 的 LGPL 许可文本，并提供获取 Pygame 源码的途径；本项目不修改上述第三方库本身，无开源义务。

## 八、下一步【补充】

- 技术栈已锁定且许可检查通过，可进入"搭建项目架构"阶段（配置、日志、错误处理、本地存储、网络模块等基础设施）
- 开发初期优先封装统一的 TCP 消息收发工具类（JSON 序列化 + 长度头分隔解决粘包），避免后期重构
- 高 DPI 通过 `ctypes.windll.user32.SetProcessDPIAware()` 在入口处处理
