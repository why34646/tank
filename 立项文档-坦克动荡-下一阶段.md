# 项目立项文档 — 坦克动荡（下一阶段）

> 本立项基于已完成的 **TankTrouble v1.0.22** 增量补充，覆盖下一阶段从 V2 开始的内容。基础框架（窗口固定 1700×1200、PyGame + pygame_gui、UDP 广播 + TCP 联机、JSON 本地存储、战绩系统）均已就绪并在 V1 验收通过。

## 一、项目说明
- 项目名称：坦克动荡（TankTrouble）— 下一阶段
- 产品类型：Windows 桌面 2D 休闲对战游戏（新增玩法功能）
- 一句话描述：在现有坦克对战基础上，优化碰墙手感，加入丰富的道具系统、操作说明界面与成就系统，让单机与局域网对战更具策略性与粘性。
- 目标用户：休闲动作对战玩家，含个人无尽模式与局域网好友对战两大人群。
- 核心痛点：当前碰墙手感僵硬（只能停住 + 禁止旋转），缺少道具变化，新手无操作指引，缺乏成长与收集激励。
- 为什么值得做：现有联机底层已稳定（v1.0.22），在稳定地基上叠加可玩性与成长线，边际成本低、玩家体验提升显著。

## 二、核心功能和边界
### V2 核心功能
1. **碰墙手感优化（贴墙滑动）**：坦克前进/后退撞墙时不再整体停死，而是沿墙方向产生可移动分量，旋转依然受限，消除卡顿感。
2. **道具系统（瞬时投射 + 负面放置）**：
   - 即时投射：机关枪、激光、穿墙激光、火箭弹、导弹、范围爆破弹
   - 负面/放置：地雷
   - 道具球有独立图标；出现与拾取播放音效；
   - 即时生效，不含限时 BUFF（限时 BUFF 归入 V3+ 扩展）。
3. **介绍界面**：入口在主菜单，单列表滚动展示基础操作（键位）与道具图标/效果说明。
4. **成就界面**：纯新增独立成就档案，达成就实时屏幕弹提示 + 音效，成就界面展示解锁状态。
5. **出生点随机化**：所有玩家（含 AI）出生改到地图随机空地，互不重叠且保持最小间距。

### V1 打好的基础（本阶段直接复用，不重做）
- 随机迷宫（4 大小、classic/carve 两种生成）、坦克/炮弹/反弹/碰撞、个人无尽模式、局域网房间、对战模式（自由/2v2/3v3）、主机权威快照同步、3 档 AI、音效、战绩存储、打包安装。

### V2 明确不做
- 不出道具"分敌我"逻辑（道具对所有坦克一视同仁，谁碰归谁）。
- 道具球不可被炮弹摧毁。
- 不做道具商店/购买、皮肤、账号、广域网。
- 不做游戏内关卡/剧情，保持"无尽轮回"玩法。

### 后期规划（V3+ 方向）
- 更多道具种类（命令式/环境互动）、成就扩展与赛季化、可选皮肤。

## 三、产品流程和用户路径
1. 用户进入主菜单 → 可选「开始游戏 / 局域网联机 / 介绍 / 成就 / 战绩」。
2. 进入单机设置或联机房间 → 配置地图/人数/AI → 开始对战。
3. 对战时：
   - 操作手感：贴墙滑动 → 操作更顺滑。
   - 地图空地周期刷新道具球 → 玩家拾取 → 播放拾取音效 → 投射/负面效果生效。
   - 出生随机点，开局冻结期内看清位置。
   - 达成成就 → 屏幕实时弹提示 + 音效。
4. 局后回主菜单 → 可查看「介绍」了解操作与道具、「成就」查看已解锁。
- 数据流：成就解锁事件写入独立成就档案；道具状态在联机下发主权威计算的包裹信息。

## 四、数据和业务对象
| 对象 | 说明 | 关键字段 |
|------|------|---------|
| 道具球 (Pickup) | 地图上刷新待拾取的实体 | id、类型、位置、生成时刻、状态(存在/被拾取/过期) |
| 道具效果 (Effect) | 已触发的一次性道具效果 | 所属坦克、类型、触发时刻、效果参数 |
| 成就记录 (Achievement) | 玩家解锁进度 | 成就id、名称、描述、条件、当前进度/目标值、是否解锁、解锁时间 |
| 出生点 (SpawnPoint) | 随机生成的对局出生位置 | x、y、占用状态、与最近坦克间距 |
| 战绩(复用) | 已有 kills/round_survived/场次 | （仅作成就触发依据，不可变于此阶段） |

## 五、技术路线
- 产品形态：Windows 桌面程序（沿用 PyGame + pygame_gui）。
- 前端方向：复用现有场景体系（MainMenu、SoloSetup、Room、Battle 等）新增「介绍」「成就」场景；道具渲染复用 Particles/地图层。
- 后端方向：无服务端；联机沿用现有 UDP 广播 + TCP + 主机权威快照模型。
- 数据库：沿用 JSON 本地存储（%APPDATA%/TankTrouble/），成就新增独立文件（如 achievements.json）。
- 关键库：pygame-ce（渲染/输入/音效）、pygame_gui（UI）、标准库 socket（联机）、标准库 json（本地存储）。
- 选型理由：V1 已用 PyGame + pygame_gui 搭建并通过验收，V2 属增量扩展；沿用可最大化复用现有场景/引擎/网络代码，避免引入新框架带来迁移风险。
- 明确排除的技术：不引入任何新外部依赖（符合项目宪法"未经批准禁止新增依赖"）；不使用独立前端框架；不需要远程后端/数据库；不新增第三方渲染或物理库。
- 重评估触发：若道具效果需要复杂物理（如穿墙激光/爆破范围判定过于复杂）或网络同步带宽超限，再评估是否需要拆组件；但 V2 优先沿用现有结构与约束完成。
- 许可说明：本项目可免费分发，Pygame 为 LGPL——沿用 V1 已处理的打包方案，本阶段不新增新版权依赖。
- 备注：道具的生成/拾取/消失/效果在联机下由房主权威计算并向客户端广播状态（与现有快照机制一致）；出生随机点同样由对局初始化阶段统一生成并入快照，保证双端一致。

## 六、差异点和项目爆点
- 相比同类粗糙复刻：在稳定联机底层上叠加成系统的"道具+手感+成长"三层体验。
- 最值得做的地方：道具系统让固定"躲炮弹"循环产生每局不同的策略，成就与介绍界面显著降低新手门槛并增强长期收集动力。

## 七、里程碑规划
| 版本 | 范围 |
|------|------|
| V1（已完成） | 固定窗口、随机迷宫、单机无尽、局域网房间/对战、AI、音效、战绩、打包 |
| V2（本阶段） | 碰墙贴墙滑动、道具系统（6 种即时投射 + 地雷 + 图标 + 音效）、出生点随机化、介绍界面、成就系统（独立档案+实时弹提示） |
| V3+ | 道具扩展、成就扩展/赛季、皮肤、更多玩法 |

## 八、验收标准
- [ ] `python main.py` 可启动，窗口固定 1700×1200 不可缩放。
- [ ] 坦克贴墙时沿墙滑动流畅、无卡顿，旋转受限且无异常弹跳。
- [ ] 地图空地按规则定时随机刷新道具球，图标正常显示，出现/拾取均有音效。
- [ ] 7 种道具（机关枪/激光/穿墙激光/火箭弹/导弹/范围爆破弹 + 地雷）拾取后效果正确：投射即时触发、地雷正确生效。
- [ ] 出生点随机、互不重叠、与坦克保持最小间距；单机与多人（含AI）均生效。
- [ ] 介绍界面可从主菜单进入，单列表展示操作与道具说明并可滚动。
- [ ] 成就界面展示解锁状态；达成时游戏内实时弹提示 + 音效。
- [ ] 联机使用本地双开验证：道具生成/拾取/消失/效果、出生点在两实例一致同步。
- [ ] 无新增异常、崩溃或运行时警告；受影响文件仅限任务范围内。